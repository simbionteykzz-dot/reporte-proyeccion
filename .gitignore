# --- Variables de entorno (NUNCA versionar credenciales) ---
# Catch-all: cualquier .env en cualquier carpeta queda excluido.
*.env
# Se permiten plantillas de ejemplo (sin secretos).
!*.env.example
!**/.env.example

__pycache__/
*.pyc
.venv/
venv/
node_modules/
frontend/dist/

# Generados / local
dashboard_payload.json
*.log
.DS_Store
Thumbs.db

# Copias locales de asistentes (muy pesadas; no son la app)
.claude/
